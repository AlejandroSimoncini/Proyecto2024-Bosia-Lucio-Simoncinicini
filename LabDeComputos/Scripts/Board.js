function modBoard(evento)
{
    var body = document.getElementyId('bodi')

    var bodiWidth = bodi.offsetWidth

    if (bodiWidth < 500) 
    {
       boardWidth = 400
    }
}

document.addEventListener('DOMContentLoaded', function() {
    modBoard();
});